program: src/train.py
args_no_hyphens: False
method: grid
metric:
  name: val/acc
  goal: minimize
parameters:
  seed:
    values: [0,1,2,3,4]
  dataset.spike:
    values: [0.1,0.25,0.5,0.75,1]

command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}